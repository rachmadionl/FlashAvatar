name: FlashAvatar
channels:
  - pytorch
  - conda-forge
  - defaults
  - nvidia/label/cuda-11.8.0
dependencies:
  - cudatoolkit
  - cuda-nvcc  # for nvcc
  - cuda-cudart-dev  # for cuda_runtime.h
  - plyfile=0.8.1
  - python=3.10
  - pip=22.3.1
  - pytorch==2.1.0
  - torchvision==0.16.0
  - torchaudio==2.1.0
  - pytorch-cuda=11.8
  - tqdm
  - pip:
    - scipy
    - chumpy
    - scikit-image
    - opencv-python
    - ninja
    - lpips
    - loguru